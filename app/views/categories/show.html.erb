<% set_meta_tags title: @category.name, og: { title: @category.name, type: 'website' }, twitter: { title: @category.name } %>
<%= render 'shared/post_list', main_title: @category.name, description: "カテゴリー #{@category.name} の記事一覧です．" %>
